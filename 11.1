#include <iostream>
#include <string>
#include <cmath>
using namespace std;
int main()
{
   int a, b, c;
    cin >> a >> b;
    
   if (a > 0 && b > 0 && a < b)
   {
        
        while (a <= b){
            for (c = 1; c <= a; c++)
       {
                cout << a;
            }
         a++;
            cout << endl;
        }
    }
    
}
