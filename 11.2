#include <iostream>
#include <string>
#include <cmath>
using namespace std;
int main()
{
    int a, b, c;
    
    cin >> a >> b;
    if (a > b)
   {
        while ((a - b) >= 0)
       {
            c = a - b;
            a = a - b;
        }
        cout << c << endl;
    }
    else
    {
        cout << "error" << ends;
    }
    
}
