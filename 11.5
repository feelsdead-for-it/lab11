#include <iostream>
using namespace std;
int main()
{

int a,b;
	
	cin >> a >> b;
	while ((a < 0) || (b < 0))
	{
		cin >> a >> b;
	}

	while (a != b) 
	{
	if (a >= b) 
		{
		    a = a - b;
		}
		else 
		    if (b >= a) 
		{
			b = b - a;
		}
	}
	cout <<"nod= "<< b;
	
}
